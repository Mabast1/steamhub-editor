import React from 'react';

import CreateClass from '../../routes/create';

export default () => {
  return (
    <div className='App'>
      {/* TODO: Split routes with react-router */}
      <CreateClass />
    </div>
  );
};
